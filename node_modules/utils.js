var initPath = function(_env)
{
    var path = "";
    if( _env === "DEV" )
    {
        path = "../public/files";
    }
    else if( _env == "DEV" )
    {
        path = "public/files";
    }
    
    return path;
}

exports.initPath = initPath;